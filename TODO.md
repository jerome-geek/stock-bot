# TODO

이 문서에는 향후 봇을 더욱 고도화하기 위한 개선 사항들을 정리합니다.

## 🚀 1. 동적 티커 관리 (스프레드시트 연동)

현재 `config/settings.py`에 하드코딩된 종목(Tickers) 정보를 스프레드시트에 위임합니다.

- [ ] 시트에 `Settings` (혹은 `Tickers`) 탭을 생성하고, 거기에 티커 심볼과 한글 종목명을 기입할 수 있도록 변경
- [ ] 봇 실행 시 `settings.py` 대신 `Settings` 시트를 우선적으로 읽어내어 분석 대상을 동적으로 결정하도록 로직 수정
- **기대 효과:** 코드를 직접 배포/수정하지 않아도 시트 관리만으로 언제든 분석 종목을 추가/삭제 가능

## 🔔 2. 텔레그램 알림(Webhook/Bot) 고도화

- [ ] 매수 조건에 도달한(Buy Signal) 종목만 별도로 강조해서 알림 전송 (예: "[긴급] SOXL 1차 매수 타점 도달!")
- [ ] 매일 고정적인 시간에 단순 요약 메시지만 보내는 것에서 벗어나, 장중 실시간 알림 기능 도입 (현재가 트리거 방식 등)
- [ ] 알림 메시지 내 가독성을 위한 마크다운 및 이모지(🟢, 🔴 등) 활용 강화

## ⚡️ 3. 예외 처리(Error Handling) 및 최적화

- [ ] `yfinance` 및 Google Sheets API에 대한 재시도(Retry) 로직 추가 (일시적인 네트워크 오류 및 Rate Limit 대응 방어 코드)
- [ ] 비동기(Async) 처리 도입 방식 검토: 다수의 티커를 병렬로 `yfinance`에서 수집하여 분석 시간 단축

## ⚙️ 4. 스케줄링 및 무인 자동화 (GitHub Actions / Cloud Scheduler)

- [ ] GitHub Actions 또는 Cloud Scheduler를 연동하여 미국장 마감 직후 (한국 시간 오전 6시~7시경) 봇이 매일 자동 실행되도록 데몬화
- [ ] CI/CD 파이프라인 구성 시 보안 키(Token, GCP Creds)를 Github Secrets나 클라우드 Secret Manager로 안전하게 분리

## 📊 5. 유연한 백테스트 및 지표 파라미터 적용

- [ ] 티커별로 서로 다른 Lookback Period(이동평균 기간)나 Z-Score 변수(1σ, 2σ, 3σ의 기준값)를 지정할 수 있도록 시트 기반 설정 추가 지원
- [ ] 단순한 레버리지 ETF 뿐만 아니라 다양한 주식 자산군에 맞춰 MDD(최대 낙폭) 등의 추가 지표를 함께 시트에 기록

## 💰 6. 계좌 포트폴리오 연동 (선택사항)

- [ ] 사용자의 실제 매입 단가와 수량을 별도의 시트 탭에 입력해두면, 현재 계좌의 총 PnL(손익)을 봇이 함께 계산해서 대시보드에 뿌려주는 기능
